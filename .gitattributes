# Собирается и запускается это на маке, поэтому переносы строк всегда LF.
# Иначе bash на маке спотыкается о \r в .command и падает с невнятной ошибкой.
* text=auto eol=lf

*.png binary
*.icns binary
*.tar.gz binary
